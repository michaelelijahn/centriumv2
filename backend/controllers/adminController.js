const pool = require('../config/db');

// This controller can be used for admin-specific operations that don't fit into other controllers
// Currently serves as a placeholder for future admin functionality
 
module.exports = {
    // Admin-specific functions can be added here in the future
}; 